function verificar(){
    var data = new Date
    var ano = data.getFullYear()
    var fano = document.getElementById('data1')
    var resu = document.getElementById('res')
    if ( fano.value.length == 0 || fano.value > ano){
        window.alert('ERRO ANO INVALIDO')
    } else{
        var sexom = document.getElementsByName('sexo')
       
     }
    }